Render Free configuration for Daneshyar

1. Rename render-free.yaml to render.yaml.
2. Replace the existing render.yaml in the repository root.
3. Commit message:
   Switch Render deployment to free public beta
4. Push to main.
5. Return to Render and retry the Blueprint.

This configuration removes the paid persistent disk and OpenAI secret requirement.
